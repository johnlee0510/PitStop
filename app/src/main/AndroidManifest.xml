<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="PitStop">
    <!-- adding internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.LoginActivity"></activity>
        <activity android:name=".Controller.RegistrationActivity" />
        <activity android:name=".Controller.MainUserActivity" />
        <activity
            android:name=".Controller.MainFoodTruckUserActivity"
            android:label="@string/title_activity_main_food_truck_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Controller.WelcomeScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.ProfileActivity"></activity>
    </application>

</manifest>